// frontend/ressources/js/config.default.js

// Check if CONFIG is already defined
window.CONFIG = window.CONFIG || {};

// Set default API_URL if not already set
window.CONFIG.API_URL = window.CONFIG.API_URL || "http://localhost:8000"; // Default for local testing
